{% extends 'base.html.twig' %}

{% block title "Recette : " ~ recipe.title %} {# si c'est que du texte on peut faire ça #}

{% block body %}
   <h1>{{ recipe.title }}</h1>

    <p>{{ recipe.content | nl2br }}</p>
   
{% endblock %}
